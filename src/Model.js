import React from 'react';
import { Modal, Button } from 'react-bootstrap';
import './Model.css';

export class ModelWindow extends React.Component {
    state = {
        show: this.props.toggle
    };
    render() {
      return (

      );
    }
  }
export default ModelWindow;